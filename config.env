# === cluster-autoinstall/config.env ===
# Edit in GitHub after your first master is up (copy values from summary).
# For your current two-node start on same LAN, you can leave these empty on the first master.

# K3s
#MASTER_URL="https://10.0.0.81:6443"
#K3S_TOKEN="REPLACE_WITH_NODE_TOKEN_FROM_FIRST_MASTER"

# WireGuard (optional; enable later if/when you want cross-site)
WG_ENABLE=false
WG_PORT=51820
WG_NET="10.100.0.0/16"
# If you enable WG and host a master behind NAT, set:
#MASTER_PUBLIC_IP="A.B.C.D"
